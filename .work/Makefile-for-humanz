.PHONY: !verchecks acl attr audit autoconf automake bash bc binutils bison brotli bzip2 check cmake coreutils curl dejagnu dhcpcd diffutils dwm e2fsprogs elfutils eudev expat expect file findutils flex fontconfig freetype-harfbuzz gawk gc gcc gdbm gettext git glib glibc gmp gnupg gperf graphite2 grep groff grub guile gzip iana-etc inetutils intltool iproute2 kbd keyutils kmod krb5 less libICE libSM libX11 libXScrnSaver libXau libXcomposite libXcursor libXdamage libXdmcp libXext libXfixes libXfont2 libXft libXi libXinerama libXmu libXrandr libXrender libXt libXtst libXxf86vm libaio libassuan libatomic_ops libbsd libcap-ng libcap libcgroup libdrm libepoxy libevdev libffi libfontenc libgcrypt libgpg-error libidn2 libinput libksba libldap libmnl libnghttp2 libnl libnsl libpciaccess libpipeline libpng libpsl libsasl libseccomp libssh2 libtasn1 libtirpc libtool libunistring libunwind libuv libxcb libxcrypt libxkbcommon libxkbfile libxshmfence linux-pam llvm logs lvm2 m4 make-ca make man-db man-pages mesa meson mkfontscale mpc mpfr mtdev nano ncurses ninja npth nspr nss openssl p11-kit patch pcre pcre2 perl pinentry pixman pkg-config procps-ng psmisc python readline sed setxkbmap shadow sowm sqlite3 st sysklogd sysvinit tar tcl texinfo tzdata udev-lfs util-linux vim wget wpa_supplicant xauth xbitmaps xcb-proto xcb-util-cursor xcb-util-image xcb-util-keysyms xcb-util-renderutil xcb-util-wm xcb-util xev xf86-input-libinput xf86-video-amdgpu xf86-video-ati xf86-video-intel xf86-video-nouveau xf86-video-vesa xinit xinput xkbcomp xkeyboard-config xml-parser xorg-fonts xorg-server xorg-util-macros xorgproto xprop xrandr xrdb xset xsetroot xtrans xz zlib zstd )
!verchecks: 
	@echo Rebuilding $@, causing $^ rebuild
acl: coreutils tar shadow vim sed gettext eudev 
	@echo Rebuilding $@, causing $^ rebuild
attr: coreutils tar acl shadow sed libcap patch 
	@echo Rebuilding $@, causing $^ rebuild
audit: linux-pam 
	@echo Rebuilding $@, causing $^ rebuild
autoconf: 
	@echo Rebuilding $@, causing $^ rebuild
automake: 
	@echo Rebuilding $@, causing $^ rebuild
bash: flex man-db pkg-config findutils bzip2 dejagnu e2fsprogs keyutils autoconf gettext libgpg-error xz grub m4 dhcpcd texinfo libtool gdbm gmp bison bison bison diffutils sysvinit pcre2 gawk automake gzip 
	@echo Rebuilding $@, causing $^ rebuild
bc: 
	@echo Rebuilding $@, causing $^ rebuild
binutils: gcc e2fsprogs iproute2 
	@echo Rebuilding $@, causing $^ rebuild
bison: 
	@echo Rebuilding $@, causing $^ rebuild
brotli: curl xorg-server freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
bzip2: file xorg-server freetype-harfbuzz python pcre2 
	@echo Rebuilding $@, causing $^ rebuild
check: 
	@echo Rebuilding $@, causing $^ rebuild
cmake: 
	@echo Rebuilding $@, causing $^ rebuild
coreutils: e2fsprogs util-linux 
	@echo Rebuilding $@, causing $^ rebuild
curl: git elfutils 
	@echo Rebuilding $@, causing $^ rebuild
dejagnu: 
	@echo Rebuilding $@, causing $^ rebuild
dhcpcd: 
	@echo Rebuilding $@, causing $^ rebuild
diffutils: autoconf 
	@echo Rebuilding $@, causing $^ rebuild
dwm: 
	@echo Rebuilding $@, causing $^ rebuild
e2fsprogs: 
	@echo Rebuilding $@, causing $^ rebuild
elfutils: binutils eudev iproute2 
	@echo Rebuilding $@, causing $^ rebuild
eudev: xf86-video-nouveau xf86-video-amdgpu xf86-video-ati xorg-server dhcpcd lvm2 xf86-video-intel util-linux libinput 
	@echo Rebuilding $@, causing $^ rebuild
expat: git xml-parser xf86-video-amdgpu mesa xf86-video-ati fontconfig freetype-harfbuzz python 
	@echo Rebuilding $@, causing $^ rebuild
expect: dejagnu 
	@echo Rebuilding $@, causing $^ rebuild
file: nano util-linux 
	@echo Rebuilding $@, causing $^ rebuild
findutils: 
	@echo Rebuilding $@, causing $^ rebuild
flex: binutils 
	@echo Rebuilding $@, causing $^ rebuild
fontconfig: st dwm xorg-server libXft 
	@echo Rebuilding $@, causing $^ rebuild
freetype-harfbuzz: st fontconfig mkfontscale grub xorg-server libXft libXfont2 
	@echo Rebuilding $@, causing $^ rebuild
gawk: check autoconf 
	@echo Rebuilding $@, causing $^ rebuild
gc: make guile 
	@echo Rebuilding $@, causing $^ rebuild
gcc: groff pcre binutils gperf gettext graphite2 libunwind llvm gc elfutils ninja gmp pcre2 
	@echo Rebuilding $@, causing $^ rebuild
gdbm: man-db libsasl python perl 
	@echo Rebuilding $@, causing $^ rebuild
gettext: e2fsprogs grub bison bison bison 
	@echo Rebuilding $@, causing $^ rebuild
git: 
	@echo Rebuilding $@, causing $^ rebuild
glib: freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
glibc: libXdmcp xinit flex libICE man-db tcl setxkbmap coreutils libXcursor libassuan git pkg-config libXmu st bash pixman libxcb groff mpc findutils libepoxy libgcrypt libXScrnSaver libX11 libnghttp2 xml-parser xf86-video-nouveau nano glib libuv attr xkbcomp libpciaccess libxkbfile xf86-video-amdgpu libpsl gcc mtdev brotli xf86-video-ati kmod fontconfig libksba bzip2 make xf86-input-libinput libffi libXfixes tar libssh2 procps-ng sysklogd acl check expat libatomic_ops shadow libidn2 libseccomp libxcrypt ncurses e2fsprogs wget pinentry libcgroup vim libXinerama xsetroot pcre libunistring keyutils dwm binutils libnsl libXrandr xcb-util-cursor mkfontscale libnl libXext file xrandr curl xcb-util-image sed krb5 libSM gperf gettext libxshmfence eudev graphite2 xset wpa_supplicant sowm libgpg-error xz libunwind xcb-util-keysyms less grub xprop zstd p11-kit xev libdrm m4 xorg-server xinput libpipeline libevdev guile libxkbcommon libXtst libXft libcap-ng xcb-util dhcpcd gnupg llvm xcb-util-wm nss libXfont2 gc elfutils sqlite3 texinfo libtool lvm2 xcb-util-renderutil inetutils grep npth mpfr gdbm freetype-harfbuzz libtirpc libsasl ninja xf86-video-intel libXau libmnl gmp libXrender psmisc libXdamage libXxf86vm util-linux bison bison bison libfontenc libinput libtasn1 diffutils zlib readline python libpng linux-pam libXcomposite perl libXt sysvinit pcre2 iproute2 expect gawk libcap patch openssl xauth xrdb xorg-fonts xf86-video-vesa kbd nspr gzip 
	@echo Rebuilding $@, causing $^ rebuild
gmp: coreutils mpc gcc vim guile mpfr gawk 
	@echo Rebuilding $@, causing $^ rebuild
gnupg: 
	@echo Rebuilding $@, causing $^ rebuild
gperf: 
	@echo Rebuilding $@, causing $^ rebuild
graphite2: xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
grep: 
	@echo Rebuilding $@, causing $^ rebuild
groff: man-db 
	@echo Rebuilding $@, causing $^ rebuild
grub: 
	@echo Rebuilding $@, causing $^ rebuild
guile: make 
	@echo Rebuilding $@, causing $^ rebuild
gzip: texinfo 
	@echo Rebuilding $@, causing $^ rebuild
iana-etc: 
	@echo Rebuilding $@, causing $^ rebuild
inetutils: 
	@echo Rebuilding $@, causing $^ rebuild
intltool: 
	@echo Rebuilding $@, causing $^ rebuild
iproute2: 
	@echo Rebuilding $@, causing $^ rebuild
kbd: eudev 
	@echo Rebuilding $@, causing $^ rebuild
keyutils: krb5 
	@echo Rebuilding $@, causing $^ rebuild
kmod: eudev 
	@echo Rebuilding $@, causing $^ rebuild
krb5: curl libtirpc libsasl 
	@echo Rebuilding $@, causing $^ rebuild
less: man-db gzip 
	@echo Rebuilding $@, causing $^ rebuild
libICE: libSM libXt 
	@echo Rebuilding $@, causing $^ rebuild
libSM: libXt 
	@echo Rebuilding $@, causing $^ rebuild
libX11: xinit setxkbmap libXcursor libXmu st libXScrnSaver xkbcomp libxkbfile mesa libXfixes libXinerama xsetroot dwm libXrandr libXext xrandr xset sowm xprop xev xorg-server xinput libXtst libXft freetype-harfbuzz xf86-video-intel libXrender libXdamage libXxf86vm libXcomposite libXt xauth xrdb 
	@echo Rebuilding $@, causing $^ rebuild
libXScrnSaver: xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXau: libxcb libX11 xcb-util-cursor xcb-util-image xorg-server libXtst xcb-util xcb-util-renderutil freetype-harfbuzz xauth 
	@echo Rebuilding $@, causing $^ rebuild
libXcomposite: 
	@echo Rebuilding $@, causing $^ rebuild
libXcursor: xsetroot xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXdamage: mesa xorg-server freetype-harfbuzz xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXdmcp: libxcb xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
libXext: libXmu libXScrnSaver mesa libXinerama libXrandr xset libXi xorg-server xinput libXtst freetype-harfbuzz xf86-video-intel libXxf86vm xauth 
	@echo Rebuilding $@, causing $^ rebuild
libXfixes: libXcursor xsetroot libXi xorg-server freetype-harfbuzz xf86-video-intel libXdamage libXcomposite 
	@echo Rebuilding $@, causing $^ rebuild
libXfont2: xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
libXft: st dwm 
	@echo Rebuilding $@, causing $^ rebuild
libXi: xinput libXtst xf86-video-vesa 
	@echo Rebuilding $@, causing $^ rebuild
libXinerama: dwm xinput xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXmu: xsetroot xset xprop xinput xauth xrdb 
	@echo Rebuilding $@, causing $^ rebuild
libXrandr: mesa xrandr xev xinput xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXrender: libXcursor mesa xsetroot libXrandr xrandr libXft freetype-harfbuzz xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXt: libXmu 
	@echo Rebuilding $@, causing $^ rebuild
libXtst: xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libXxf86vm: mesa xorg-server freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
libaio: lvm2 
	@echo Rebuilding $@, causing $^ rebuild
libassuan: pinentry gnupg 
	@echo Rebuilding $@, causing $^ rebuild
libatomic_ops: gc 
	@echo Rebuilding $@, causing $^ rebuild
libbsd: 
	@echo Rebuilding $@, causing $^ rebuild
libcap-ng: 
	@echo Rebuilding $@, causing $^ rebuild
libcap: coreutils shadow pinentry eudev libcap-ng inetutils util-linux iproute2 
	@echo Rebuilding $@, causing $^ rebuild
libcgroup: 
	@echo Rebuilding $@, causing $^ rebuild
libdrm: xf86-video-nouveau xf86-video-amdgpu mesa xf86-video-ati xorg-server freetype-harfbuzz xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libepoxy: xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
libevdev: libinput 
	@echo Rebuilding $@, causing $^ rebuild
libffi: glib vim p11-kit guile python 
	@echo Rebuilding $@, causing $^ rebuild
libfontenc: mkfontscale xorg-server libXfont2 
	@echo Rebuilding $@, causing $^ rebuild
libgcrypt: gnupg 
	@echo Rebuilding $@, causing $^ rebuild
libgpg-error: libassuan libgcrypt libksba pinentry gnupg 
	@echo Rebuilding $@, causing $^ rebuild
libidn2: libpsl wget curl 
	@echo Rebuilding $@, causing $^ rebuild
libinput: xf86-input-libinput 
	@echo Rebuilding $@, causing $^ rebuild
libksba: gnupg 
	@echo Rebuilding $@, causing $^ rebuild
libldap: krb5 
	@echo Rebuilding $@, causing $^ rebuild
libmnl: iproute2 
	@echo Rebuilding $@, causing $^ rebuild
libnghttp2: curl 
	@echo Rebuilding $@, causing $^ rebuild
libnl: wpa_supplicant 
	@echo Rebuilding $@, causing $^ rebuild
libnsl: python linux-pam perl 
	@echo Rebuilding $@, causing $^ rebuild
libpciaccess: xf86-video-amdgpu xf86-video-ati libdrm xorg-server xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
libpipeline: man-db 
	@echo Rebuilding $@, causing $^ rebuild
libpng: freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
libpsl: wget curl 
	@echo Rebuilding $@, causing $^ rebuild
libsasl: libldap 
	@echo Rebuilding $@, causing $^ rebuild
libseccomp: man-db file 
	@echo Rebuilding $@, causing $^ rebuild
libssh2: curl 
	@echo Rebuilding $@, causing $^ rebuild
libtasn1: p11-kit 
	@echo Rebuilding $@, causing $^ rebuild
libtirpc: libnsl python linux-pam 
	@echo Rebuilding $@, causing $^ rebuild
libtool: guile 
	@echo Rebuilding $@, causing $^ rebuild
libunistring: libpsl libidn2 wget gettext guile 
	@echo Rebuilding $@, causing $^ rebuild
libunwind: xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
libuv: 
	@echo Rebuilding $@, causing $^ rebuild
libxcb: libX11 mesa xcb-util-cursor xcb-util-image xcb-util-keysyms xorg-server libxkbcommon libXtst xcb-util xcb-util-wm xcb-util-renderutil freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
libxcrypt: shadow guile inetutils libsasl util-linux python linux-pam perl sysvinit 
	@echo Rebuilding $@, causing $^ rebuild
libxkbcommon: 
	@echo Rebuilding $@, causing $^ rebuild
libxkbfile: setxkbmap xkbcomp xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
libxshmfence: mesa xorg-server freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
linux-pam: shadow libcgroup libsasl 
	@echo Rebuilding $@, causing $^ rebuild
llvm: mesa 
	@echo Rebuilding $@, causing $^ rebuild
logs: 
	@echo Rebuilding $@, causing $^ rebuild
lvm2: grub 
	@echo Rebuilding $@, causing $^ rebuild
m4: flex autoconf bison bison bison 
	@echo Rebuilding $@, causing $^ rebuild
make-ca: wget p11-kit xorg-fonts 
	@echo Rebuilding $@, causing $^ rebuild
make: 
	@echo Rebuilding $@, causing $^ rebuild
man-db: 
	@echo Rebuilding $@, causing $^ rebuild
man-pages: 
	@echo Rebuilding $@, causing $^ rebuild
mesa: xf86-video-amdgpu xf86-video-ati xorg-server freetype-harfbuzz 
	@echo Rebuilding $@, causing $^ rebuild
meson: 
	@echo Rebuilding $@, causing $^ rebuild
mkfontscale: 
	@echo Rebuilding $@, causing $^ rebuild
mpc: gcc 
	@echo Rebuilding $@, causing $^ rebuild
mpfr: mpc gcc gawk 
	@echo Rebuilding $@, causing $^ rebuild
mtdev: libinput 
	@echo Rebuilding $@, causing $^ rebuild
nano: 
	@echo Rebuilding $@, causing $^ rebuild
ncurses: bash nano procps-ng pinentry vim pcre gettext wpa_supplicant less guile gnupg llvm sqlite3 texinfo lvm2 inetutils gdbm psmisc util-linux readline python gawk 
	@echo Rebuilding $@, causing $^ rebuild
ninja: meson 
	@echo Rebuilding $@, causing $^ rebuild
npth: gnupg 
	@echo Rebuilding $@, causing $^ rebuild
nspr: nss 
	@echo Rebuilding $@, causing $^ rebuild
nss: make-ca p11-kit 
	@echo Rebuilding $@, causing $^ rebuild
openssl: coreutils git kmod libssh2 wget curl wpa_supplicant xorg-server libsasl python xorg-fonts 
	@echo Rebuilding $@, causing $^ rebuild
p11-kit: make-ca nss 
	@echo Rebuilding $@, causing $^ rebuild
patch: 
	@echo Rebuilding $@, causing $^ rebuild
pcre: grep 
	@echo Rebuilding $@, causing $^ rebuild
pcre2: git wget util-linux 
	@echo Rebuilding $@, causing $^ rebuild
perl: git groff autoconf texinfo intltool automake openssl 
	@echo Rebuilding $@, causing $^ rebuild
pinentry: gnupg 
	@echo Rebuilding $@, causing $^ rebuild
pixman: xorg-server freetype-harfbuzz xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
pkg-config: 
	@echo Rebuilding $@, causing $^ rebuild
procps-ng: 
	@echo Rebuilding $@, causing $^ rebuild
psmisc: 
	@echo Rebuilding $@, causing $^ rebuild
python: meson 
	@echo Rebuilding $@, causing $^ rebuild
readline: bash pcre wpa_supplicant guile gnupg sqlite3 lvm2 inetutils gdbm python bc pcre2 gawk 
	@echo Rebuilding $@, causing $^ rebuild
sed: 
	@echo Rebuilding $@, causing $^ rebuild
setxkbmap: 
	@echo Rebuilding $@, causing $^ rebuild
shadow: git util-linux 
	@echo Rebuilding $@, causing $^ rebuild
sowm: 
	@echo Rebuilding $@, causing $^ rebuild
sqlite3: gnupg nss python 
	@echo Rebuilding $@, causing $^ rebuild
st: 
	@echo Rebuilding $@, causing $^ rebuild
sysklogd: 
	@echo Rebuilding $@, causing $^ rebuild
sysvinit: 
	@echo Rebuilding $@, causing $^ rebuild
tar: libtool 
	@echo Rebuilding $@, causing $^ rebuild
tcl: 
	@echo Rebuilding $@, causing $^ rebuild
texinfo: gperf 
	@echo Rebuilding $@, causing $^ rebuild
tzdata: glibc 
	@echo Rebuilding $@, causing $^ rebuild
udev-lfs: 
	@echo Rebuilding $@, causing $^ rebuild
util-linux: e2fsprogs wget eudev lvm2 python 
	@echo Rebuilding $@, causing $^ rebuild
vim: 
	@echo Rebuilding $@, causing $^ rebuild
wget: 
	@echo Rebuilding $@, causing $^ rebuild
wpa_supplicant: 
	@echo Rebuilding $@, causing $^ rebuild
xauth: 
	@echo Rebuilding $@, causing $^ rebuild
xbitmaps: xsetroot 
	@echo Rebuilding $@, causing $^ rebuild
xcb-proto: libxcb 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util-cursor: 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util-image: xcb-util-cursor 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util-keysyms: 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util-renderutil: xcb-util-cursor 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util-wm: 
	@echo Rebuilding $@, causing $^ rebuild
xcb-util: xcb-util-cursor xcb-util-image 
	@echo Rebuilding $@, causing $^ rebuild
xev: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-input-libinput: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-video-amdgpu: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-video-ati: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-video-intel: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-video-nouveau: 
	@echo Rebuilding $@, causing $^ rebuild
xf86-video-vesa: 
	@echo Rebuilding $@, causing $^ rebuild
xinit: 
	@echo Rebuilding $@, causing $^ rebuild
xinput: 
	@echo Rebuilding $@, causing $^ rebuild
xkbcomp: xkeyboard-config 
	@echo Rebuilding $@, causing $^ rebuild
xkeyboard-config: xorg-server 
	@echo Rebuilding $@, causing $^ rebuild
xml-parser: intltool 
	@echo Rebuilding $@, causing $^ rebuild
xorg-fonts: 
	@echo Rebuilding $@, causing $^ rebuild
xorg-server: xf86-input-libinput xf86-video-intel 
	@echo Rebuilding $@, causing $^ rebuild
xorg-util-macros: libxcb 
	@echo Rebuilding $@, causing $^ rebuild
xorgproto: libXdmcp libX11 xf86-video-nouveau xf86-video-amdgpu mesa xf86-video-ati libXinerama 
	@echo Rebuilding $@, causing $^ rebuild
xprop: 
	@echo Rebuilding $@, causing $^ rebuild
xrandr: 
	@echo Rebuilding $@, causing $^ rebuild
xrdb: 
	@echo Rebuilding $@, causing $^ rebuild
xset: 
	@echo Rebuilding $@, causing $^ rebuild
xsetroot: 
	@echo Rebuilding $@, causing $^ rebuild
xtrans: 
	@echo Rebuilding $@, causing $^ rebuild
xz: kmod file libunwind grub zstd elfutils python 
	@echo Rebuilding $@, causing $^ rebuild
zlib: man-db tcl git nano glib mesa gcc kmod libssh2 wget vim pcre binutils mkfontscale file curl libunwind zstd xorg-server gnupg llvm libXfont2 elfutils sqlite3 freetype-harfbuzz util-linux libfontenc python libpng pcre2 
	@echo Rebuilding $@, causing $^ rebuild
zstd: gcc curl elfutils 
	@echo Rebuilding $@, causing $^ rebuild
